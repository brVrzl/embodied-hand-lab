data_root: data/episodes
structured_export_root: data/exports/structured
lerobot_export_root: data/exports/lerobot
episode:
  default_operator: operator
  save_rgb: true
  save_depth: true
  image_format: npy
  write_jsonl_flush: true
sampling:
  arm_hz: 10
  hand_hz: 5
  dog_hz: 10
  camera_hz: 10
labels:
  success_values:
    - success
    - failure
    - partial_success

