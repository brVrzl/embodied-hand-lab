task_name: visual_approach
description: Use visual feedback to approach a target object or region.
required_sensors:
  - rgb
  - depth
  - dog_states
success_condition: Robot reaches the approach threshold without triggering estop.
recommended_logging_fields:
  - target_description
  - stop_distance_m
  - approach_timeout_sec

