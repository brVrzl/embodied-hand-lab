task_name: object_reorient
description: Grasp an object and change its orientation to a desired pose.
required_sensors:
  - rgb
  - depth
  - hand_states
  - arm_ee_pose
success_condition: Final object orientation matches the requested orientation tolerance.
recommended_logging_fields:
  - initial_orientation
  - target_orientation
  - final_orientation

