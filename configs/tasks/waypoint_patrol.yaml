task_name: waypoint_patrol
description: Move the quadruped through a list of waypoints and return state logs.
required_sensors:
  - dog_states
success_condition: Robot reaches all waypoints within timeout and returns to final checkpoint.
recommended_logging_fields:
  - waypoint_list
  - patrol_duration_sec
  - localization_health

